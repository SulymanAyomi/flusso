// import { Models } from "node-appwrite";


export type WorkspaceType = {
    name: string;
    id: string;
    imageUrl: string;
    inviteCode: string;
    ownerId: string;
    createdAt: string | Date;
    updatedAt: string | Date;
}
