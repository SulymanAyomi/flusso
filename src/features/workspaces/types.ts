export type WorkspaceType = {
    name: string;
    id: string;
    imageUrl: string | null;
    inviteCode: string;
    ownerId: string | null;
    createdAt: string | Date;
    updatedAt: string | Date;
}
